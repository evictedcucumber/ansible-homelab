[OPTIONS]
enable: 1
policy_in: DROP
policy_out: DROP

[IPSET management]
{% for item in firewall_management_ipset %}
{{ item }}
{% endfor %}

[RULES]
IN SSH(ACCEPT) -source +management
IN ACCEPT -p tcp -dport 8006 -source +management

OUT DNS(ACCEPT)
OUT HTTP(ACCEPT)
OUT HTTPS(ACCEPT)
