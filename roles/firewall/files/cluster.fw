[OPTIONS]
enable: 1
policy_in: DROP
policy_out: DROP

[IPSET management]
192.168.1.29

[RULES]
IN SSH(ACCEPT) -source +management
IN ACCEPT -p tcp -dport 8006 -source +management

OUT DNS(ACCEPT)
OUT HTTP(ACCEPT)
OUT HTTPS(ACCEPT)
